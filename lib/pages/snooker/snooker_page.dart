import 'package:flutter/material.dart';

class SnookerPage extends StatelessWidget {
  const SnookerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
